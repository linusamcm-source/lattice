//! Lattice backend binary entry point.

fn main() {
    println!(
        "lattice backend (protocol {}) — scaffold; see docs/orignal_specs/BUILD_PLAN.md",
        lattice_backend::protocol_sentinel()
    );
}
